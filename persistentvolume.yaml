apiVersion: v1
kind: PersistentVolume
metadata:
  name: shared-app-pv-ping-pong
  labels:
    app: shared-storage
spec:
  storageClassName: local-storage
  capacity:
    storage: 100Mi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  local:
    path: /tmp/kube
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k3d-log-output-cluster-agent-0
  claimRef:
    namespace: ping-pong-cluster
    name: shared-storage-claim
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: shared-app-pv-log-output
  labels:
    app: shared-storage
spec:
  storageClassName: local-storage
  capacity:
    storage: 100Mi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  local:
    path: /tmp/kube
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k3d-log-output-cluster-agent-0
  claimRef:
    namespace: log-output-cluster
    name: shared-storage-claim